<!-- Navigation -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">Almacen Loco</a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
              <li>
                  <a href="/recipes">Recetas</a>
              </li>
              <li>
                  <a href="/ingredients">Ingredientes</a>
              </li>
              <li>
                  <a href="/dishes">Platillos</a>
              </li>
              <li>
                  <a href="/subingredients">Almacen</a>
              </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <% if logged_in? %>
                <li><a href="/logout">Logout</a></li>
              <% else %>
                <li><a href="/login">Login</a></li>
              <% end %>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>
<div class="index">
  <p id="notice"><%= notice %></p>

  <h1>Listing Dishes</h1>
  <hr>
  <table  class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Cost</th>
      </tr>
    </thead>

    <tbody>
      <% @dishes.each do |dish| %>
        <tr>
          <td><%= dish.name %></td>
          <td><%= dish.cost %></td>
          <% if logged_in? && current_user.role == 1%>
            <td><%= link_to 'Edit', edit_dish_path(dish) , :class=>'btn-sm btn-warning' %></td>
          <% end %>
          <% if logged_in? && (current_user.role == 1 || current_user.role ==2) %>
            <td><%= link_to 'Preparar', new_sub_path(:nom => dish.name) , :class=>'btn-sm btn-primary' %></td>
          <% end %>
          <% if logged_in? && current_user.role == 3 %>
            <td><%= link_to 'Ordenar', new_sub_path(:nom => dish.name) , :class=>'btn-sm btn-primary' %></td>
          <% end %>
          <% if logged_in? && current_user.role == 1%>
            <td><%= link_to 'Destroy', dish, method: :delete, data: { confirm: 'Are you sure?' } , :class=>'btn-sm btn-danger' %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

</div>
